/** Re-export so both `@/lib/site-config` and `@/content/site-config` resolve. */
export { siteConfig } from "@/content/site-config";
export type { SiteConfig, Service } from "@/content/site-config";
