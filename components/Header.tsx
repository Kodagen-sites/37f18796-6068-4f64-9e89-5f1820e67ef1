"use client";

// Header router — siteConfig.headerVariant resolves to transparent-ghost.
import HeaderTransparentGhost from "./headers/HeaderTransparentGhost";

export default function Header() {
  return <HeaderTransparentGhost />;
}
